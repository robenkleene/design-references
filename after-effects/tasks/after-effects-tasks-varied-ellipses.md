# After Effects Tasks Varied Ellipses

- `⌥LMB` on stroke to switch to gradient, then `LMB` to set stroke (opacity and color)
- `Layer > Contents > Add Trim Paths`: To fade in / out the start / end
